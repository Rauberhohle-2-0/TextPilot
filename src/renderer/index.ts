/**
 * The renderer: the app's UI, built as plain-DOM features.
 *
 * No TSX. A `dom.ts` element factory keeps markup declarative and typed,
 * and `features/` holds one folder per feature. `boot.ts` composes them
 * into the app shell - adding a feature to the UI is a new folder and one
 * line in the shell, nothing else.
 */
